# 🎯 AutoTestHub 面试准备指南

> 这份文档是针对**测试开发/高级测试工程师**面试场景的项目问答准备。
> 结合你的简历（中国银行 DevOps 云平台 + Locust/Playwright/流水线经验），逐题准备。

---

## 📋 第一部分：项目概述（30秒电梯演讲）

### 面试官："请简单介绍一下这个项目"

**标准话术：**

> "这是我独立开发的 AI 驱动的全链路测试平台，叫 AutoTestHub。
>
> 核心理念是：**把 AI 能力嵌入到测试全流程**，从用例生成、执行、质量分析，到知识沉淀，形成闭环。
>
> 技术栈是 Django REST + Vue3 + PostgreSQL + ChromaDB + 通义千问。
> 支持 Docker Compose 一键部署，有 GitHub Actions CI/CD 流水线。
>
> 我在银行项目里深刻体会到**用例维护成本高、测试知识流失、AI 应用质量难以评估**这三个痛点，
> 所以用这个平台把解决方案产品化了。"

---

## 📋 第二部分：高频问题逐题准备

### 🔴 必问 1："为什么做这个项目？解决什么问题？"

**参考回答：**

> "我做了 3 件事：
>
> **第一，AI 生成用例**——银行项目接口文档多，手工写用例效率低。
> 我做了个功能：上传接口文档 → AI 解析字段和逻辑 → 自动生成用例 + 断言规则。
> 效率大概提升 60-80%。
>
> **第二，质量数字人**——AI 生成的用例怎么保证质量？我设计了三维度评分引擎：
> 完整性(40分) + 格式规范(20分) + 内容质量(40分)。低于60分自动标记，人工复核。
>
> **第三，AI 测评师**——当我们在项目里用 AI 做问答时，怎么知道 AI 答得对不对？
> 我做了个自动评估 + 安全检测的工具，可以对任何 AI 做批量评测。"

### 🔴 必问 2："架构怎么设计的？为什么选这些技术？"

**参考回答：**

> "前后端分离：Vue3 + Django REST Framework。Nginx 做统一入口和反向代理。
>
> **技术选型理由：**
> - Django：ORM 成熟，适合快速开发；DRF 序列化器写 API 很方便
> - ChromaDB：轻量级向量数据库，嵌入式部署，不需要额外服务，适合小团队
> - 通义千问：中文能力强，API 稳定，国内可访问
> - Pytest + Allure：测试行业标准，报告美观，面试官应该都知道
> - Docker Compose：一键部署 3 个服务（PostgreSQL + Django + Nginx）"

### 🔴 必问 3："测试执行引擎怎么实现的？"

**参考回答：**

> "核心思路是**动态生成 Pytest 脚本**。
>
> 流程：
> 1. 用户选择用例 → 创建执行记录
> 2. Engine 读取用例的 method、URL、headers、body、断言规则
> 3. 动态生成 .py 文件，把用例信息写入 Python 代码
> 4. subprocess 调 pytest 运行，收集输出和 Allure 结果
> 5. 解析 case_results.json 得到每个用例的 pass/fail/error
>
> **亮点设计：**
> - 接口串联：extract_rules 从响应中提取变量（JSONPath/Regex），通过 context_vars 传给后续接口
> - 变量替换：支持 `${var}` 和 `{{var}}` 两种语法，递归替换 headers、body、URL
> - 安全回退：如果 allure 不可用，自动回退到普通 pytest 模式
> - 超时保护：subprocess timeout 600秒，防止卡死"

### 🔴 必问 4："质量数字人的评分逻辑是什么？"

**参考回答：**

> "三个维度，满分 100：
>
> **完整性检查（40分）：**
> - 功能测试：标题、步骤、预期结果、前置条件是否齐全
> - 接口测试：URL、Method、请求参数、预期响应是否齐全
>
> **格式规范检查（20分）：**
> - 标题长度（>=5字）、是否模糊关键词（"正常""没问题"等）
> - 步骤是否编号、URL 格式、HTTP Method 合法性
>
> **内容质量检查（40分）：**
> - 是否有具体验证关键词（验证/断言/等于/包含）
> - 是否覆盖边界条件、异常场景
> - 接口测试是否校验了 status_code 和响应体关键字段
>
> **额外：重复检测**——用 SequenceMatcher 比较全文相似度 ≥85% 标记重复，扣15分。
>
> **等级：** pass(≥80) / warning(60-79) / fail(<60)"

### 🔴 必问 5："AI 测评师做了什么？怎么评估 AI 回答的质量？"

**参考回答：**

> "这是一个**用 AI 评估 AI** 的工具。
>
> **流程：**
> 1. 创建测评任务，添加问题列表（支持期望答案对比）
> 2. 批量向目标 AI 提问 → 收集回答
> 3. 用通义千问评估每个回答的正确性、完整性、相关性、表述质量
> 4. 安全检测：4 类风险——prompt 注入、有害内容、数据泄露、偏见毒性
> 5. 生成多维度报告（准确率、响应速度、安全性、回答质量）
>
> **亮点：**
> - 支持两种目标类型：知识库机器人（RAG）和自定义 API
> - 安全检测是两层：关键词匹配 + AI 深度分析
> - 综合评分公式：准确率 50% + 平均分 20% + 速度 15% + 安全 15%
> - 自动生成改进建议（准确率低、响应慢、安全风险等）"

### 🔴 必问 6："RAG 知识库怎么实现的？"

**参考回答：**

> "标准 RAG 架构：
>
> 1. **文档处理**：支持 PDF/DOCX/TXT 上传，PyPDFLoader + docx2txt 提取文本
> 2. **文本分块**：RecursiveCharacterTextSplitter，chunk_size=500，overlap=50
> 3. **向量化**：通义千问 text-embedding-v3 模型
> 4. **存储**：ChromaDB 持久化，每个知识库独立 collection
> 5. **检索**：相似度搜索 Top-3，拼接 context → LLM 生成答案
>
> **双模式：**
> - Knowledge 模式：基于知识库检索回答，返回参考文档
> - Chat 模式：直接 LLM 对话，支持图片输入（多模态）
>
> **选择 ChromaDB 的原因**：嵌入式部署，不需要额外数据库服务，适合演示和小团队。"

### 🟡 高频 7："项目里用到了哪些设计模式？"

**参考回答：**

> "几个关键的设计模式：
>
> - **策略模式**：QualityCheckEngine 通过 scenario 参数区分功能测试/接口测试，
>   不同场景有不同的必填字段和扣分规则
> - **模板方法**：TestExecutionEngine._generate_test_file() 定义脚本生成骨架，
>   具体用例数据通过参数注入
> - **工厂模式**：数据工厂模块的 PresetTemplate + DataFactoryTemplate，
>   通过模板配置批量生成测试数据
> - **观察者模式**：AI 测评师的 progress_callback 机制"

### 🟡 高频 8："Docker Compose 怎么设计的？为什么这么分层？"

**参考回答：**

> "三个服务：
>
> 1. **db（PostgreSQL 16 Alpine）**：轻量镜像，healthcheck 确保就绪
> 2. **backend（Django + Gunicorn）**：多阶段构建，pip 依赖单独一层利用 Docker 缓存；
>    depends_on db + condition: service_healthy 保证启动顺序
> 3. **frontend（Nginx + Vue）**：两阶段构建，npm build → nginx 托管静态文件；
>   nginx 反向代理 /api/ 到 backend:8000
>
> **为什么这么分？**
> - 数据持久化：postgres_data + media_data + allure_data 三个 volume
> - 环境变量控制：DEBUG/DB_ENGINE/CORS_ALLOW_ALL 通过 .env 切换
> - 开发/生产切换：开发挂载代码热更新，生产用镜像部署"

### 🟡 高频 9："CI/CD 流水线做了什么？"

**参考回答：**

> "GitHub Actions，3 个 Job：
>
> 1. **backend-tests**：安装 Python 依赖 → migrate 验证 → 跑全量单元测试
> 2. **frontend-build**：npm ci → npm run build 验证前端能正常打包
> 3. **docker-build**：验证 Dockerfile 能构建 + docker compose config 检查
>
> 触发条件：push 到 main/develop、PR 到 main、手动触发
>
> **我在银行项目维护流水线**的经验是：触发条件、环境变量、并行任务、构建缓存这些概念是一样的。
> 只是银行用的是 Jenkins，这里是 GitHub Actions，本质都是 CI 流程。"

### 🟡 高频 10："项目有什么不足？如果继续做会怎么优化？"

**参考回答：**

> "几个明确的改进方向：
>
> 1. **性能测试集成**：我有 Locust 实战经验，计划加一个性能测试模块，和现有执行引擎打通
> 2. **UI 自动化集成**：我有 Playwright 经验，想加入 UI 自动化录制回放
> 3. **定时任务**：现在执行引擎预留了 scheduled 触发方式，需要接入 Celery/APScheduler
> 4. **通知告警**：加钉钉/企微 Webhook，执行完成自动推送
> 5. **测试覆盖率**：核心模块有 59 个单测，但覆盖率还不够，需要继续补充
> 6. **代码质量**：可以加 ESLint + Black + mypy 做代码规范检查"

---

## 📋 第三部分：技术深挖问题

### 🔥 "subprocess 执行 Pytest 有什么坑？"

> "三个坑：
> 1. **Python 环境问题**：用 _find_python_with_packages() 找到安装了 requests+pytest 的 Python
> 2. **编码问题**：Windows 下 subprocess 输出可能乱码，需要设置 encoding='utf-8'
> 3. **超时问题**：timeout=600 秒，超时会抛 TimeoutExpired，需要捕获处理"

### 🔥 "JSONPath 提取怎么实现的？变量替换支持嵌套吗？"

> "自己实现了一个简化版 JSONPath：按 '.' 分割路径，逐层取值，支持数组索引。
> 变量替换支持 `${var}` 和 `{{var}}` 两种语法，递归替换 dict/list/str，
> 但不支持嵌套变量如 `${${a}}`，目前没这个需求。"

### 🔥 "ChromaDB 的 chunk_size 怎么选的？overlap 为什么是 50？"

> "chunk_size=500 是实践出来的：太小语义不完整，太大会引入噪声。
> overlap=50（10%）确保上下文连贯，不会因为分块导致语义断裂。
> 这个值可以根据文档类型调整，技术文档可以大一点，对话类文档可以小一点。"

### 🔥 "通义千问的 temperature 为什么设 0.1？"

> "评测场景需要稳定一致的输出，temperature 越低越确定。
> 0.1 基本是确定性的，适合做质量评估。日常对话模式用的是 0.7，更有创造性。"

### 🔥 "JWT Token 怎么管理的？"

> "双 Token 机制：Access Token 24h + Refresh Token 7d。
> 前端在 axios 拦截器里自动附加 Authorization header。
> Access 过期自动用 Refresh 换新的，不需要用户重新登录。"

### 🔥 "为什么用 SQLite 开发、PostgreSQL 生产？"

> "SQLite 零配置，开发阶段不需要安装数据库服务，快速迭代。
> PostgreSQL 支持并发写入、JSON 字段索引、全文搜索，适合生产环境。
> 通过环境变量 DB_ENGINE 一键切换，settings.py 里用 if/else 分支。"

---

## 📋 第四部分：项目亮点总结（面试官会记住的 5 个点）

| # | 亮点 | 一句话 |
|---|------|--------|
| 1 | **AI 全链路闭环** | 用例生成→执行→质量分析→知识沉淀→AI评测，5个环节AI增强 |
| 2 | **动态 Pytest 脚本生成** | 不是静态脚本，是根据数据库里的用例实时生成的 |
| 3 | **质量数字人** | 三维度 100 分制评分，20+ 条扣分规则，功能/接口双场景 |
| 4 | **AI 测评师** | 用 AI 评估 AI，安全检测 + 多维度报告，银行项目直接能用 |
| 5 | **工程化完整** | Docker Compose 一键部署 + GitHub Actions CI + 59 个单元测试 |

---

## 📋 第五部分：薪资谈判话术

### 你的筹码：

| 筹码 | 话术 |
|------|------|
| 银行项目经验 | "金融级测试合规要求，350+ 功能用例，200+ 插件测试" |
| AI+测试 | "不是纸上谈兵，有可演示的产品，能说清楚技术细节" |
| 全栈能力 | "前后端都能写，不需要依赖前端同学" |
| 工程化 | "Docker/CI/单测/架构文档都有" |
| 性能+自动化 | "Locust 压测 + Playwright UI + JMeter 都会" |

### 目标薪资：

| 城市 | 目标 | 底线 |
|------|------|------|
| 深圳 | 28-35K | 25K |
| 北京/上海 | 28-38K | 25K |
| 杭州/成都 | 22-28K | 18K |

### 谈薪话术：

> "我在银行项目里积累了金融级测试经验，同时独立开发了 AI 驱动的全链路测试平台。
> 我的技术栈覆盖了自动化测试、性能测试、AI 应用、全栈开发。
> 我对自己的定位是**能独立从 0 到 1 搭建测试体系的中级测开**。
> 目前市场上的对标岗位薪资在 25-35K 区间，我希望在这个范围内。"

---

## 📋 第六部分：项目状态速查

| 维度 | 状态 |
|------|------|
| **后端框架** | Django 4.2 + DRF |
| **前端** | Vue3 + Vite + Element Plus |
| **数据库** | SQLite(开发) / PostgreSQL(生产) |
| **向量数据库** | ChromaDB |
| **AI 引擎** | 通义千问 DashScope (text-embedding-v3 + qwen-vl-plus + qwen-plus) |
| **测试引擎** | Pytest + Allure |
| **部署** | Docker Compose (3 服务) |
| **CI/CD** | GitHub Actions (3 Jobs) |
| **单元测试** | 59 个 (testcases 15 + execution 14 + quality_checker 19 + ai_evaluator 11) |
| **前端页面** | 12 个 (登录/看板/用例/执行/套件/问答/数据工厂/Agent/质量/测评) |
| **后端模块** | 8 个 Django App |

---

## 📋 第七部分：面试快速复习清单

### 面试前一天晚上必看：

- [ ] 30 秒项目概述背熟
- [ ] 执行引擎流程图在脑子里过一遍
- [ ] 质量数字人三维度 + 扣分规则过一遍
- [ ] AI 测评师安全检测 4 类风险记住
- [ ] RAG 知识库 5 步流程记住
- [ ] Docker Compose 3 个服务 + 依赖关系记住
- [ ] CI/CD 3 个 Job 记住
- [ ] 自己的简历项目经验复习一遍（银行 DevOps 项目）
- [ ] 薪资底线心里有数

### 面试当天：

- [ ] 如果能带电脑，提前把项目跑起来（docker compose up -d）
- [ ] 准备好浏览器打开 localhost 可以演示
- [ ] 如果远程面试，准备好屏幕共享
- [ ] 准备几个数据演示：创建用例 → 加入套件 → 执行 → 看报告

---

> **祝你面试顺利！你的能力匹配 25-35K，自信一点。**
